<p style="color: green"><%= notice %></p>

<h1>Restaurants</h1>


<% if @restaurants.present? %>
  <ul id="restaurants">
    <% @restaurants.each do |restaurant| %>
      <li><%= render restaurant %> | <%= link_to "More infos", restaurant %></li>
      
    <% end %>
  </ul>
<% else %>
  <div>No restaurants to display 😬</div>
<% end %>


<%= link_to "Add a restaurant", new_restaurant_path %>
